import { Button } from '@mui/material';
import { React } from 'react';

const MIButton = () =>
	<Button
		sx={ { width: 200 } }
		variant="contained"
		color="primary"
		size="small"
	>Primary</Button>;

export default MIButton;
